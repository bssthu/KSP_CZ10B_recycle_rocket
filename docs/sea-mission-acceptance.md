# 真实海上全任务验收

## 2026-07-16：严格 2 km、低空角速度与 30 秒持有（最终实机通过）

最终静音全任务从 0% 油门开始点火，5 秒线性爬升到 100%，两个 TT18-A 只在实测 TWR 大于 1.05 后释放。一级以 19.94% 推进剂余量分离；二级只执行一次燃烧并在 `apoapsis=110162 periapsis=90454` 截止，之后推力保持为零。

一级在 130 m / 13 m/s 条件锁存高空横向收尾，并在 1 km 以下切换到柔和转向响应。最终 `TERMINAL_2KM vertical=112.20 horizontal=1.23 error=22.3 nozzleAngleMax=22.05`，通过 200/5/30 的严格门限；1 km/500 m 以下 0.35 秒低通角速度峰值为 8.83/6.84°/s，均低于 10°/s，首次进入中心后的最大回摆为 10.5 m。

捕获提交后，低燃料垂直控制从挂点 65 m 内连续保持 6.0 m/s，消除了旧走廊先减速、18 m PID 再加速造成的燃料浪费。最终四点捕获为 `vertical=-5.97 lateral=0.77 tilt=1.3`，着网余量 0.18%，最大捕获偏移 4.90 m。一个主索约束三轴角运动，其余三索保留柔性并共同放缆到 11.92 m；一级连续稳定 30 秒后报告 `SEA_MISSION_TEST_PASS`，随后又保持约 50 秒，未发生迟发横移、脱索、下坠、回收船侧翻或约束异常。

同步后的确定性点质量回归为 250/250 捕获：最差入网垂直 6.018 m/s、横向 0.248 m/s、位置误差 3.674 m，150 m 以下悬停时间为 0；237/250 工况满足中心回摆不超过 8 m 的附加判据。离线模型不替代上述 KSP 完整任务日志。

## 0.6.1：单向慢速锁存与提前横向整定（最终实机通过）

对 2026-07-14 遥测重新分段后，`FINAL_ALIGN` 的实际交接状态为挂点高度 62.81 m、横速 5.03 m/s、误差 7.62 m、倾角 15.4°；25.3 m 最大中心回摆主要发生在这个交接之后。0.6.1 因此把慢速横向最终整定提前到“500 m 以下且误差不超过 30 m”，同时新增独立的 `FINAL_ALIGN_VERTICAL_CONTROL_HEIGHT=65 m`：横向提前吸收姿态滞后，垂直下降计划和低燃料 4.0 m/s 下穿时机保持不变。

第一次完整复验进一步暴露出旧的 300 m 慢速场会把约 8.5 m/s 的收敛状态重新加速到 30 m/s，最终以 `TERMINAL_2KM vertical=56.04 horizontal=69.63 error=292.1 nozzleAngleMax=166.66` 失败。修正后，慢速状态锁存进入瞬间的径向速度作为后续上限，并保留 10 m/s 的最低收敛权限，不再在内圈重新加速。

第二次静音完整复验在 2026-07-16 通过。一级仍以 19.94% 余量分离，二级进入 107.694 × 90.443 km 轨道；2 km 门限为垂直 120.00 m/s、横向 4.70 m/s、误差 29.8 m，最大喷管/速度夹角 15.24°。垂直捕获在挂点高度 484.79 m、横速 1.33 m/s、误差 4.93 m、倾角 0.4°时锁存；一级最终以垂直 −3.98 m/s、横向 0.30 m/s、倾角 0.3°四点捕获。插件逐物理帧记录的最大中心回摆由 25.3 m 降至 10.8 m（0.2 秒遥测摘要可能漏掉短时峰值），着网余量由 0.11% 提高到 0.35%，拉索放缆到 11.92 m 后连续稳定超过 8 秒并报告 `SEA_MISSION_TEST_PASS`。

更新后的 250 组确定性回归仍为 250/250 捕获，最差入网垂直 1.499 m/s、横向 0、位置误差 8.641 m，150 m 以下悬停时间为 0；最差中心回摆为 19.835 m，234/250 满足回摆不超过 8 m 的附加判据。

## 2026-07-14：低轨道、固定落点轨迹与无过冲捕获（最终实机通过）

2026-07-14 的最终真实海上任务在 `MASTER_VOLUME = 0` 下完成 KSC 当地 06:00 发射。一级约在 49 km、推进剂余量 19.94% 时分离，二级进入 107.693 × 90.444 km 轨道；一级返回自然落区后以 `vertical=-4.00 lateral=0.33 tilt=4.5` 四点捕获，着网余量 0.11%。拉索连续放缆到 11.92 m 后，一级横纵速度接近 0，平台倾斜 0.04°，严格验收器在连续稳定超过 8 秒后报告 `SEA_MISSION_TEST_PASS`。

本轮针对反馈完成以下修复：

- 自动任务推进到下一次 KSC 当地 06:00；回收船改放在纬度约 −0.05865°、经度约 −27.6947° 的当轮自然落区，水下地形约 −1114.2 m，平台实测倾斜 0.04°。
- 一级改用独立 stock Mainsail，喷焰、烟迹和发射台烟尘均由原版效果负责；两个 stock TT18-A 下移到脚部接地高度，并继续以 `srfAttach` 连接一级。一级与二级分离前，一级发动机 `THRUSTLIMIT` 先置零，分离完成后才恢复。
- 一级在 500 m 开始重力转向，35 km 完成；35 km 以下地速上限为 720 m/s。分离余量保持在 20% 以内，二级载荷增重后仍由自适应逻辑完成 100 km 远地点、90 km 近地点入轨。
- 一级分离后上升滑行使用纯角速度阻尼，实测最大角速度仅 0.58°/s。下降到 50 km 后按计划转向表面逆行，40 km 执行入口减速；再入计划动作峰值 19.55°/s，与分离后的上升稳定性分开统计。
- 30 km 建立一次高度索引轨迹，13.5 km 内进入单向停车走廊；常规垂直制动限于 75% 最大推力、合成矢量限于 98%，速度不低于 30 m/s 时喷管/速度夹角限于 30°。最终实飞的 2 km 门限为垂直 104.86 m/s、横向 3.05 m/s、误差 18.9 m，整个受约束高速段最大夹角 16.28°；5 km 以下首次进入中心 10 m 后的最大回摆为 25.3 m。
- 挂点到网面 65 m 内边下降边执行有界最终对准，误差小于 7 m、横速小于 1.25 m/s、倾角小于 8°后锁存垂直下穿；不设置定时悬停，推进剂低于 2% 时保持 4.0 m/s 安全下穿。500 m 以下横向控制不得额外提高垂直推力，避免修正过冲时向上跳升或在网面上方耗尽燃料。
- 拉索在目标首次进入上方 100 m、24 m × 20 m 区域后锁存目标并逐物理帧随动；索口按高度连续缩小，在网面上方 1 m 达到最终尺寸。捕获后的单帧关节冲量由 1.5 秒角速度低通滤除，但 8 秒连续低速、低倾角稳定门槛没有放宽。

Python 3.10 与 3.13 的 250 组确定性回归均为 250/250 捕获：最差入网垂直 1.499 m/s、横向 0、位置误差 7.223 m，150 m 以下悬停时间为 0；最差中心回摆 19.835 m，234/250 工况满足中心回摆不超过 8 m 的更严格判据。最终实机在 2 km 后把水平速度由 3.05 m/s 收敛到捕获时的 0.33 m/s，中心回摆峰值 25.3 m，全程无 `REJECT`，捕获后无甩飞或回收船侧翻。

## 0.6.0 反馈轮最终实机验收

2026-07-12，0.6.0 在 KSP 1.12.5 中以 `MASTER_VOLUME = 0` 完成自动真实海面验收。火箭位于发射台中心，回收船初始位于侧面；组合载具先等待 8 秒物理解包，再把船部署到纬度约 −0.09720、经度约 −71.05939、距发射点约 36.6 km 的深水海面。发动机裸机顶部与一级本体直接相接，全部外罩关闭；两个 stock TT18-A 与一级表面连接，并在实测 `TWR=1.406` 时播放释放动作。

### 结构与机构

- 一级 Mainsail 裸机按其 `+0.533 m` 顶节点重新定位到 Rockomax64 的 `−3.73 m` 底面，消除了原先 0.48 m 的可见断口；`fairingSmall` 也关闭。
- 火箭分支保持在发射台中心，回收船根零件向西偏置 18 m；一级喷管底部约有 1 m 地面间隙，TT18-A 径向位置向箭体收紧。
- 回收船四个塔头新增四根横梁，形成闭合矩形顶框；静态运输抱夹继续收纳在一侧塔柱。
- 四条三节点柔索在闭合期间连续跟踪挂点中心，只有 `inset` 和中心跟踪误差都收敛后才报告 `Closed`。捕获改为挂点真正穿到网面附近后触发，0.75 m 受载下沉预量驱动弹簧阻尼关节和视觉柔索一起下挠。

### 飞行与制导

- 上升按 Kerbin 600 km 半径和 70 km 大气层尺度，把程序转弯从 20° 增至 45°；18 km 分离后上面级沿分离方向继续飞行。
- 一级不再在垂直速度接近零时追随快速旋转的速度方向，而是保持分离惯性姿态；下降速度达到 −25 m/s 后才做一次 6 秒平滑翻转。固定滑行角速度约 0.04–0.37°/s，计划翻转是全程唯一的高角速度动作，峰值约 25.17°/s。
- 单向返航点火在约 45 秒结束，末段水平速度约 41 m/s；随后用 6 秒无动力转到固定竖直姿态，实测转向峰值约 14.6°/s。一次再入点火只渐入油门，不再追随速度方向或把喷口摆回。
- 10 km 入口进入固定到达时间的受约束三次轨迹，入口误差约 347 m、横速约 27.6 m/s；500 m 以下切近场 PID。实际一路连续下降，未用低空悬停平移弥补远端轨迹误差。

### 最终证据

- 任务日志：`SEA_MISSION_TEST_PASS powered=True high=True separated=True hook=Captured maxHighAngular=25.17`。
- 捕获日志：`hooks=4 vertical=-0.63 lateral=0.09 tilt=0.0`。
- 最后数值样本：挂点高度 0.19 m、垂直 −0.628 m/s、横向 0.086 m/s、中心误差 1.00 m、质量 15.822 t。
- 100 m 以下最大制导倾角 0.2°，油门饱和比例 0.0%；12–150 m 区间没有近似悬停时间。
- 柔索状态依次为 `Open → Closing → Closed → Captured`，闭索跟踪误差收敛到 0.00 m；捕获后记录 `sag=0.62`，稳定时为 `sag=0.63 m`。
- Python 3.10 与 3.13 的 250 组扩展回归均为 250/250；最差入口垂直 0.640 m/s、横向 0.005 m/s、误差 0.068 m，150 m 以下悬停时间 0.000 s。
- 安装后的 KSP 原生验证解析五个模板，零件数为 4、7、4、1、3；无标记生产启动到达主菜单，10 个 CZ10B 零件全部编译，`ERR=0`、`EXC=0`、自动任务触发数为 0。

物理捕获门槛保持不变：四挂点全部进入网窗、垂直速度不超过 7.5 m/s、横向速度不超过 4 m/s、倾角不超过 15°，且柔索必须完成闭合与中心跟踪。

## 0.5.0 最终实机验收

2026-07-12，0.5.0 在 KSP 1.12.5 中以 `MASTER_VOLUME = 0` 完成自动真实海面验收。`CZ10B Full Mission Recovery Test` 从 KSC 发射；观察器把一件式 `Recovery Ship` 部署到纬度约 −0.09721、经度约 −72.05767、距发射点约 26.18 km 的深水海面。PQS 海床高度约 −984.7 m，平台保持真实水面物理状态，并非陆地平台或冻结的位置替身。

### 飞行与机构链路

- 初始整箭和回收船同在发射台。回收船先分离并部署，10 秒后一级点火；两个真实 stock `TT18-A` 继续抱持，实际推重比超过 1.02 后才释放。
- 火箭只执行 20° 常规重力转弯，在约 18 km 进行两级分离；上面级发动机点燃并沿分离前方向继续飞行。
- 一级零油门滑行越过约 38.5 km 远地点，随后翻转并执行返航点火；下落阶段只执行一次再入/中段制动，没有水平越过目标或喷口朝天的大幅摆动。
- 6.5 km 以下进入受推力、横向加速度和倾角约束的末制导。高度 427 m 时距目标仍约 128 m，控制器先平稳归中，再以约 0.65 m/s 穿越网面。
- 一级进入塔架后，四条三节点柔索依次显示 `Open → Closing → Closed`；捕获后柔索中点随箭体向下挠曲，四个弹簧阻尼关节吸能。塔架一侧另保留静态收拢抱夹，表达捕获后的转运固定机构。

### 最终证据

- 任务日志：`SEA_MISSION_TEST_PASS powered=True high=True separated=True hook=Captured`。
- 捕获日志：`hooks=4 vertical=-0.62 lateral=0.02 tilt=0.0`。
- 遥测入口样本：挂点高度 1.56 m、垂直 −0.62 m/s、横向 0.02 m/s、中心误差 0.09 m。
- 100 m 以下最大倾角 4.2°，油门饱和比例 0.0%；捕获后保持超过 8 秒才判定通过。
- 观察器在任务 UT 400.3 秒记录 `net=Closing`，405.3 秒记录 `net=Closed`，420.3 秒记录 `hook=Captured`；稳定验收在约 424.6 秒完成。
- 离线扩展回归在 Python 3.10 与 3.13 下分别保持 250/250，最差入口约为垂直 0.619 m/s、横向 0.006 m/s、位置误差 0.069 m。
- 安装后的 KSP 原生验证成功解析五个模板，零件数分别为 4、7、4、1、3；随后无标记生产启动正常到达主菜单，10 个 CZ10B 零件全部编译，日志统计为 `ERR=0`、`EXC=0`，且未自动触发任何测试。

物理捕获门槛没有为本次成功放宽：四个挂点必须全部进入网窗，垂直速度不超过 7.5 m/s、横向速度不超过 4 m/s、倾角不超过 15°；并且捕获只会在柔索完全闭合后发生。

## 0.4.0 历史基线

0.4.0 首次证明了自动真实海面闭环可行：一级完成 18 km 两级分离、boost-back 和末段归中，以 `vertical=-0.55 lateral=1.46 tilt=1.8` 四点捕获并稳定到 `SEA_MISSION_TEST_PASS`。该版本仍余约 3.9 t 推进剂，排除了燃料耗尽后碰撞入网的假成功。0.5.0 在此基础上改进了整箭外形、真实夹具、船体/塔架/闭索机构和完整飞行时序，并显著降低最终横向速度与中心误差。
